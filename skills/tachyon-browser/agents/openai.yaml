interface:
  display_name: "Tachyon Browser"
  short_description: "Use the k3s Mesh CDP browser gateway"
  default_prompt: "Use $tachyon-browser to open a temporary remote browser session and verify a page."
